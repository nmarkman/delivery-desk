import React from 'react';
import { Layout } from '@/components/Layout';
import { SyncDashboard } from '@/components/SyncDashboard';

export default function ActSync() {
  return (
    <Layout>
      <SyncDashboard />
    </Layout>
  );
} 